export interface IStatistic {
  courses: number;
  mentors: number;
  mentees: number;
}

export interface ILeaderBoard {
  id: string;
  name: string;
  email: string;
  score: number;
  avatar: string;
  rank: number;
}
